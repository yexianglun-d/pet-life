spring:
  application:
    name: petlife-server
  jackson:
    property-naming-strategy: SNAKE_CASE
  datasource:
    url: ${PETLIFE_DATASOURCE_URL:jdbc:mysql://124.70.176.192:3306/pet_life?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${PETLIFE_DATASOURCE_USERNAME:root}
    password: ${PETLIFE_DATASOURCE_PASSWORD:Abc123!@#}
    driver-class-name: com.mysql.cj.jdbc.Driver
# 暂不配置redis
#  data:
#    redis:
#      host: ${PETLIFE_REDIS_HOST:127.0.0.1}
#      port: ${PETLIFE_REDIS_PORT:6379}
#      password: ${PETLIFE_REDIS_PASSWORD:}
  flyway:
    enabled: false

server:
  port: ${PETLIFE_SERVER_PORT:8080}
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  level:
    root: INFO
    com.petlife.server: INFO
