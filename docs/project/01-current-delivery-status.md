# Current Delivery Status

## 文档目的

该文档用于按“完整交付标准”记录 `PetLife / 宠物生活管家` 当前真实进度。

从本次校准开始：

- 不再把“主链路打通”视为模块完成。
- 不再把“骨架页 / 占位页 / 只跑通前后端一半”的状态记为完成版。
- 除用户明确延后的 `commerce`、`device` 外，其余范围都按 `mobile-app + server + admin-web` 三端完整交付推进。

## 范围基线

当前完整交付范围：

- 用户端 App
- 服务端
- 后台管理端

当前明确允许延后：

- 商城真实交易后端
- 设备厂商接入与设备事件链路

## 当前总判断

当前项目已经完成了大量真实代码和多条业务主链路，但如果按“完整交付标准”判断：

- 健康档案、萌宠日常已经满足当前范围内的完整交付标准。
- 多数业务域仍处于 `部分完成`。
- 当前项目状态应定义为：
  - 已进入正式业务开发阶段
  - 已完成较大规模真实实现
  - 距离完整完成版仍有明显缺口

## 已进入真实开发的业务域

以下业务域已经具备真实代码基础，不再属于纯设计或纯骨架状态：

- 认证与会话
- 当前用户与个人中心
- 家庭共养
- 宠物主档
- 首页驾驶舱
- 健康档案
- 提醒计划
- 萌宠日常
- 成长时间轴
- 社区
- 举报与审核处理
- 服务中心
- 通知与消息

## 仍然存在的主要缺口

### 1. 用户端 App

- 登录增强已补齐验证码发送、倒计时、自动续期、登录失效恢复和移动端去 `mocked_code` 适配；登录页已移除固定验证码展示与自动填充，真实短信供应商仍未接入。
- 移动端已完成无网络、请求超时、会话过期、服务端异常和返回结构异常分类；refresh token 临时网络异常不会误清本地会话。
- 个人中心已补齐设置页、城市切换、通知与权限设置、消息中心；通知设置已明确当前控制站内消息与提醒偏好，不承诺系统 Push 或短信能力。
- 宠物主档已补齐完整详情、扩字段、删除/归档、无宠物空状态、后台宠物查询页和宠物问题修复入口。
- 首页已补齐快捷记录、提醒中心、周报、月报，并已改为消费首页专用聚合接口。
- 健康档案已补齐疫苗、驱虫、体检、用药、体重、异常观察专用表单、下一次提醒入口和附件选择/上传/预览。
- 提醒计划已补齐用户端模板读取、模板选择、表单预填、创建、完成、跳过和周期提醒状态机。
- 萌宠日常已接入服务端媒体资源引用与预览元数据回显，并补齐用户端图片/视频选择、上传、预览和删除。
- 社区用户端已补齐推荐、关注、同城、问答四个内容流，独立发布页、帖子详情、话题页、问答详情页、关注 / 取消关注交互和审核状态展示已接入真实接口；发布后审核状态刷新和被拒绝后的重提入口仍未完成。
- 服务中心已补齐用户端首页、服务商列表/详情、评价展示、预约提交、预约记录、取消预约、已完成预约评价入口、定位距离展示、距离排序和外部地图导航；后台城市开通配置、服务商资料/项目/时段维护、预约状态处理、评价治理、操作审计和地图坐标排查已完成；服务端已接入高德 Web 服务配置、地理编码/逆地理编码辅助、服务商坐标维护和基于用户经纬度的距离返回/排序。App 内嵌地图导航和后台 Web JS 地图选点未纳入本轮。
- 地图上线收口验收尚未通过：服务端已补齐部分迁移状态下的 `idx_service_providers_coordinate_source` 索引兼容与增量 SQL，但 8080 运行实例仍需确认最新代码，真实 `PETLIFE_AMAP_WEB_SERVICE_KEY` 未注入，测试库缺少可验证距离排序的服务商数据，Android/iOS 真机定位与外部导航验收未执行；移动端已补高德 Key 本地注入说明、Key 缺失态和外部导航 fallback 异常兜底。
- 通知消息已补齐站内消息中心、系统/提醒/预约列表、模板化通知字段展示、已读状态、服务端消息模板管理、通知渠道配置和通知配置审计接口；服务端已提供 Push token 注册契约但移动端尚未接入系统 Push 权限与 token 上报。
- 移动端风险确认弹层、高频表单校验错误态、保存/删除/预约/评价/设置成功反馈和陪伴式加载骨架已统一；Snackbar / Toast 全量深层组件化仍需继续收口。

### 2. 服务端

- 认证链路已补齐 refresh、退出登录、随机短信验证码 hash 校验、频控、错误次数锁定、过期和使用后失效；当前通过 `dev_noop` 供应商记录发送受理状态，真实短信供应商 SDK 仍未接入。
- 用户资料编辑、城市切换、通知设置、后台用户列表/详情查询、通知列表、已读状态、提醒完成通知和审核结果通知已完成。
- 家庭共养主链路、后台家庭列表/详情查询、家庭停用/恢复和 owner 成员关系修复接口已完成。
- 宠物删除/归档、更多字段扩展、无宠物空状态、后台宠物列表/详情查询和问题数据修复接口已完成。
- 健康记录扩展字段、附件 asset_id 写入回显、媒体上传校验、附件预览元数据、家庭成员记录反查读取权限、自动提醒派生和后台健康记录查询接口已完成；对象存储/CDN 已完成配置与元数据预留，真实云厂商上传适配器仍未完成。
- 萌宠日常媒体引用、预览元数据、社区同步和后台日常内容查询接口已完成；成长时间轴已补齐健康、日常、服务预约派生和后台事件排查接口。
- 社区服务端已补齐独立发布、媒体资产引用、帖子详情、关注 / 取消关注 / 关注状态、话题页、问答详情、后台帖子和问答内容治理、治理审计写入与查询；公开内容发布和日常同步已进入审核任务，公开流仅展示已通过内容，第三方内容审核仍未接入。
- 系统提醒后台列表/详情查询已完成，支持状态、类型、模式、宠物、家庭、主人、处理人、来源健康记录和提醒时间筛选；提醒模板模型、后台列表/详情/创建/更新/启停接口，以及用户端宠物可用模板读取接口已完成。
- 服务中心目录、城市开通配置、时段、预约创建/取消、预约记录、预约评价、评价状态治理、操作审计、预约通知、时间轴派生和后台运营接口已完成；高德 Web 服务底座、后台地图配置状态/地理编码/逆地理编码接口、服务商坐标维护接口、服务商列表 `distance_meters`/`sort=distance`、移动端定位、距离排序和外部地图导航已完成。
- 通知配置已补齐 `message_templates` 后台 CRUD/启停、`notification_channel_configs` 后台 CRUD/启停和通知配置审计查询；站内欢迎、提醒、预约、审核结果通知已改为优先读取启用的 `inbox` 消息模板；Push 设备 token、任务和投递记录底座已落地，移动端已完成 token 注册/解绑仓储适配，但不真实获取 SDK token 或发送系统 Push。
- 第三方能力仍未接入：真实短信供应商、对象存储、内容审核供应商、Push 推送供应商等。高德 Web 服务已作为服务端 HTTP 底座接入，移动端已接入高德定位 SDK 和外部地图导航，但地图上线收口仍需真实 Key 注入、真实服务商数据和真机验收。

### 3. 后台管理端

- 认证已切换为真实后台账号体系，后台端已接入登录、退出和刷新接口；普通 App 用户 token 不能访问后台接口。
- 短信验证码排查页已接入发送记录与校验记录后台查询接口，列表手机号脱敏展示，详情不展示明文验证码、`code_hash` 或 `salt`。
- 审核中心已接入举报处理；社区帖子治理页和问答治理页已接入列表筛选、详情、下架/恢复和治理审计查询；审核任务页已接入真实任务列表、详情、审核快照、回调 payload、任务审计和人工通过/拒绝。
- 用户、家庭、宠物、健康、萌宠日常、时间轴、系统提醒查询、提醒模板管理、社区内容治理、消息模板管理和通知渠道配置后台页面已按 OpenAPI 接入。
- 通知发送配置页只维护站内信、短信、Push 渠道配置状态；Push 投递排查页已接入任务与投递记录查询、状态排查、失败原因和设备标识脱敏展示；真实短信供应商和 Push 推送通道仍未接入。
- 服务商后台已进入真实维护页并补齐城市配置、评价治理、操作审计、地图配置状态、地理编码辅助、坐标维护和距离能力排查；后台未接 Web JS 地图，不展示地图画布或地图选点。

## 进度记账原则

后续进度统一按以下规则描述：

- `已完整完成`：功能在当前范围内满足完整交付标准。
- `部分完成`：已有真实实现，但仍缺关键页面、关键接口、治理能力、异常态或必要联动。
- `未开始`：仅有设计、占位、骨架或尚未进入实现。
- `当前预留`：仅用于 `commerce` 与 `device`，或用户后续明确指定延后的范围。

详细功能级进度以 `docs/project/02-feature-completion-checklist.md` 为准。
